{
  "name": "storage",
  "version": "1.0.0",
  "description": "A utillity to access the local/session storage in the browser",
  "main": "storage.js",
  "scripts": {
    "hint": "jshint ./src/*.js",
    "uglify":"uglifyjs ./build/storage/.js -o ./build/storage.min.js",
    "build": "browserify ./src/index.js -s Storage -o ./build/storage.js"
  },
  "author": "Christopher Smith",
  "license": "OML",
  "devDependencies": {
    "browserify": "13.0.0",
    "lodash": "4.12.0",
    "uglify-js": "2.6.1"
  }
}
